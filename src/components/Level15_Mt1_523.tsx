'use client'; 

import React from "react";

export default function Level15_Mt1_523() {
  return (
    <div className="mt-1 text-sm leading-snug">You can also replace Frame.io, Air, &amp; Recharm with our 0% markup creative storage, but feel free to do this later (we integrate with them).</div>
  );
}